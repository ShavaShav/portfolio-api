# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and never committed.

# ---------------------------------------------------------------------------
# Chat completions provider
# ---------------------------------------------------------------------------
# The OpenAI SDK is used for all providers. Switch provider by changing these
# three env vars — no code changes needed.
#
# Option A: OpenRouter (recommended free option)
#   Sign up at openrouter.ai, create a key, browse free models at
#   openrouter.ai/models?q=:free
#
# AI_BASE_URL=https://openrouter.ai/api/v1
# AI_API_KEY=sk-or-...
# AI_MODEL=google/gemma-3-27b-it:free
#
# Option B: HuggingFace Inference (free tier)
#   Create a fine-grained token at hf.co/settings/tokens
#
# AI_BASE_URL=https://router.huggingface.co/v1
# AI_API_KEY=hf_...
# AI_MODEL=meta-llama/Llama-3.3-70B-Instruct
#
# Option C: OpenAI (original, paid)
#
# AI_BASE_URL=   (leave unset)
# AI_API_KEY=sk-...
# AI_MODEL=gpt-4o-mini

AI_BASE_URL=https://openrouter.ai/api/v1
AI_API_KEY=
AI_MODEL=google/gemma-3-27b-it:free

# ---------------------------------------------------------------------------
# Embeddings (only needed for `npm run precompute`, not at runtime)
# ---------------------------------------------------------------------------
# Run `npm run precompute` once after changing your dataset files.
# The result is committed to dataset/embeddings.json and loaded at runtime.
#
# IMPORTANT: EMBEDDINGS_BASE_URL and EMBEDDINGS_MODEL must stay consistent
# between precompute runs and production — the same model must embed both
# documents (precompute) and queries (runtime) or similarity scores break.
#
# Option A: HuggingFace feature-extraction (free, recommended)
#   Uses a different API shape from OpenAI — handled automatically when
#   the URL contains "huggingface.co".
#
EMBEDDINGS_BASE_URL=https://router.huggingface.co/hf-inference/models
EMBEDDINGS_API_KEY=hf_...
EMBEDDINGS_MODEL=BAAI/bge-small-en-v1.5
#
# Option B: OpenAI (one-time cost, best accuracy)
#
# EMBEDDINGS_BASE_URL=https://api.openai.com/v1
# EMBEDDINGS_API_KEY=sk-...
# EMBEDDINGS_MODEL=text-embedding-3-small
